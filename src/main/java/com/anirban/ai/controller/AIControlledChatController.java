package com.anirban.ai.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/anirban/controlled/chat")
public class AIControlledChatController {
	
	/*
	 * @Qualifier("openApiRestTemplate")
	 * 
	 * @Autowired private RestTemplate restTemplate;
	 * 
	 * @Value("${openai.model}") private String model;
	 * 
	 * @Value("${openai.api.url}") private String apiUrl;
	 */
	

}
